criteria_value_1={{ .criteria_value_1 | shellquote }}
set -euo pipefail
log="$criteria_value_1"
if git ls-files --error-unmatch "$log" 2>/dev/null; then
    git add "$log"
    git commit -m "chore: update review log"
fi
