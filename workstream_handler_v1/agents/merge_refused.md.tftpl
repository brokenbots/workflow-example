The merge was refused. Diagnose and resolve it before anything else.

{{ .stdout }}

If unresolved_threads is non-zero, read each thread: address real findings as code changes, and reply-then-resolve anything already handled or out of scope.