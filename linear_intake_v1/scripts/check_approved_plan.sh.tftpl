criteria_value_1={{ .intake_root | shellquote }}
criteria_value_2={{ .ticket_id | shellquote }}
set -euo pipefail
intake_root="$criteria_value_1"
ticket_id="$criteria_value_2"

if [ -f "${intake_root}/${ticket_id}/approved-plan.md" ]; then
    printf '%s' "true"
else
    printf '%s' "false"
fi
