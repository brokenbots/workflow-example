diff --git a/internal/adapter/environment/sandbox/linux.go b/internal/adapter/environment/sandbox/linux.go
index 68ffc51..132306e 100644
--- a/internal/adapter/environment/sandbox/linux.go
+++ b/internal/adapter/environment/sandbox/linux.go
@@ -137,7 +137,7 @@ func (h Handler) Prepare(ctx PrepareContext) (LinuxPrepared, error) {
 		mode = "permissive"
 	}
 
-	prep := LinuxPrepared{Mode: mode}
+	prep := LinuxPrepared{Mode: mode, TargetPath: ctx.AdapterBinary}
 	prep.SysProcAttr = buildSysProcAttr()
 
 	readPaths, writePaths, netAllow, allowNet, err := extractPolicyPaths(ctx.Policy)
@@ -144,6 +144,9 @@ func (h Handler) Prepare(ctx PrepareContext) (LinuxPrepared, error) {
 	if err != nil {
 		return LinuxPrepared{}, err
 	}
+	if allowNet {
+		prep.SysProcAttr.Cloneflags &^= syscall.CLONE_NEWNET
+	}
 	prep.AllowNetwork = allowNet
 
 	if err := applyLandlockToPrep(&prep, ctx.Caps, mode, readPaths, writePaths, netAllow); err != nil {
diff --git a/internal/adapterhost/sessions.go b/internal/adapterhost/sessions.go
--- a/internal/adapterhost/sessions.go
+++ b/internal/adapterhost/sessions.go
@@ -968,7 +968,8 @@ func makeSandboxCustomizer(prep *sandbox.LinuxPrepared, envNode *workflow.Enviro
 			cmd.SysProcAttr = nil
 		}, cleanup
 	}
 	return func(_ string, cmd *exec.Cmd) {
+		prep.TargetPath = cmd.Path
 		_ = prep.ApplyToCmd(cmd, os.Args[0])
 	}, cleanup
 }