# Routes ConfigMap — the operator configuration of record for Criteria
# workflow routing (CRI-216). Introduced by ADR-0005's D1/D2 workflow source
# model; consumed by operator routing from CRI-223 onward.
#
# The single data key 'routes.json' holds the payload. Its schema of record is
# k8s/routes.schema.json; k8s/tests/test_routes_config.sh validates this
# example against that schema. The payload is JSON (a valid YAML scalar inside
# the ConfigMap) so it can be validated without a YAML parser.
#
# ADR-0005 mode mapping (declared, never inferred):
#   type=image            -> image-only: workflow baked into the image.
#   type=url (no image)   -> url-only: fetched at admission, minimal runtime.
#   type=url + image      -> url+image: URL is content, image is process.
#
# The 'linear-intake-v1' library object and the 'linear-intake-url' object
# keep the legacy intake behavior reachable: runs execute the baked
# linear_intake_v1 image, or its url-only fetch, via a ticket's
# workflows-label-group override — no route assigns them by default anymore.
#
# CRI-243 (plan CRI-214 M10.1, exit condition 3): the criteria project's
# routes are the split pattern — its actual workflow. The 'criteria-triage'
# route fires linear_triage_v1 on [Triage] and the 'criteria-develop' route
# fires linear_develop_v1 on [Ready for Development], both url-only via git
# URL with ADR-0005 D7 commit pins:
#   - linear-triage-url is pinned to current main (91153bc, CRI-311 loop
#     recovery 2026-09-23) — the triage/develop tree content is identical to
#     the earlier CRI-240 pin. Its
#     class is 'triage' (CRI-242/M9.5): triage work is read-only against the
#     repo, so triage runs admit concurrently with any run on the same
#     repoURL while dev runs serialize per repoURL.
#   - linear-develop-url is pinned to current main (91153bc); class stays
#     omitted (defaults to dev).
# CRI-310 (plan CRI-214 M10.4, carrier for CRI-246, validation run C): the
# dirty label is a ROUTING SURFACE. The 'ticket-cleanup-url' object fetches
# the ticket_cleanup_v1 tree (url-only, no image, on criteria-base — the
# CRI-244 validation-run-a child shape: single shell adapter, no git
# operations, so its only secret is the Linear API key) and the
# 'criteria-cleanup' route fires it on tickets carrying the watcher's sticky
# "criteria-dirty" label in [Ready for Development]. The tag-based route
# beats the tag-less general develop route by the specific-over-general
# priority (internal/routes/routes.go matchRoute). Its class is 'triage'
# (CRI-242): the cleanup pass is read-only against the repo.
# The object ships WITHOUT a 'ref' pin deliberately: this repository squash
# merges (the shipped pins all name main-landed commits — set in FOLLOW-UP
# changes CRI-241/CRI-243 once the squashed merge commit existed and was
# unguessable before). A
# follow-up change must pin 'ref' to the merge commit that lands
# ticket_cleanup_v1, per the same "any change to a subtree must re-pin its
# object's 'ref'" rule below; until then the fetch resolves the tree at the
# default branch head exactly like the pinless linear-intake-url object.
# Any change to either subtree must re-pin its object's 'ref' to the new
# resolved commit SHA — ADR-0005 D7 enforces the pin fail-closed at
# admission. The 'Ready for Development' state must exist on the CRI team
# before any triage run reaches that terminal:
# k8s/create-ready-for-development-state.sh creates it idempotently via the
# Linear API (type=unstarted — the work is refined but not begun), and the
# Makefile 'apply-routes' target documents the deploy ordering that pairs
# the operator/watcher rollout with the state creation and this cutover.
#
# Secrets are name references only: every secret here points at a
# SecretProviderClass (OpenBao via the Secrets Store CSI driver). No
# credential material appears in this file (plan CRI-214 section 3.8).
apiVersion: v1
kind: ConfigMap
metadata:
  name: criteria-routes
  namespace: criteria-jobs
  labels:
    app.kubernetes.io/name: criteria-routes
    app.kubernetes.io/managed-by: criteria-k8s
data:
  routes.json: |
    {
      "apiVersion": "criteria.brokenbots.dev/v1",
      "kind": "Routes",
      "workflowLibrary": {
        "linear-intake-v1": {
          "type": "image",
          "image": "localhost:5000/linear-intake-remote:dev",
          "namespace": "criteria-jobs",
          "volumes": [
            {
              "name": "data",
              "kind": "pvc",
              "claim": "criteria-data",
              "mountPath": "/data",
              "env": {
                "CRITERIA_RUN_DIR_ROOT": "/data/.criteria/runs",
                "CRITERIA_SHELL_ALLOWED_PATHS": "/data"
              }
            },
            {
              "name": "repo",
              "kind": "pvc",
              "claim": "criteria-repo",
              "mountPath": "/repo"
            }
          ],
          "secrets": [
            {
              "name": "linear-api-key",
              "secretProviderClass": "linear-spc",
              "mountPath": "/home/criteria/linear-secrets",
              "env": {
                "LINEAR_API_KEY": "linear_api_key"
              }
            },
            {
              "name": "github-tokens",
              "secretProviderClass": "copilot-spc",
              "mountPath": "/home/criteria/secrets",
              "env": {
                "WORKFLOW_GITHUB_TOKEN": "workflow_github_token",
                "REVIEWER_GITHUB_TOKEN": "reviewer_github_token"
              }
            }
          ]
        },
        "linear-intake-url": {
          "type": "url",
          "url": "git::https://github.com/brokenbots/workflow-example.git//linear_intake_v1",
          "namespace": "criteria-jobs",
          "volumes": [
            {
              "name": "data",
              "kind": "pvc",
              "claim": "criteria-data",
              "mountPath": "/data",
              "env": {
                "CRITERIA_RUN_DIR_ROOT": "/data/.criteria/runs",
                "CRITERIA_SHELL_ALLOWED_PATHS": "/data"
              }
            },
            {
              "name": "repo",
              "kind": "pvc",
              "claim": "criteria-repo",
              "mountPath": "/repo"
            },
            {
              "name": "scratch",
              "kind": "tmp",
              "mountPath": "/tmp/scratch",
              "sizeLimit": "8Gi"
            }
          ],
          "secrets": [
            {
              "name": "linear-api-key",
              "secretProviderClass": "linear-spc",
              "mountPath": "/home/criteria/linear-secrets",
              "env": {
                "LINEAR_API_KEY": "linear_api_key"
              }
            },
            {
              "name": "github-tokens",
              "secretProviderClass": "copilot-spc",
              "mountPath": "/home/criteria/secrets",
              "env": {
                "WORKFLOW_GITHUB_TOKEN": "workflow_github_token",
                "REVIEWER_GITHUB_TOKEN": "reviewer_github_token"
              }
            }
          ]
        },
        "linear-develop-url": {
          "type": "url",
          "url": "git::https://github.com/brokenbots/workflow-example.git//linear_develop_v1?ref=91153bc90b8fb48cec215c2a4cbc425dd11b7821",
          "ref": "91153bc90b8fb48cec215c2a4cbc425dd11b7821",
          "namespace": "criteria-jobs",
          "volumes": [
            {
              "name": "data",
              "kind": "pvc",
              "claim": "criteria-data",
              "mountPath": "/data",
              "env": {
                "CRITERIA_RUN_DIR_ROOT": "/data/.criteria/runs",
                "CRITERIA_SHELL_ALLOWED_PATHS": "/data"
              }
            },
            {
              "name": "repo",
              "kind": "pvc",
              "claim": "criteria-repo",
              "mountPath": "/repo"
            },
            {
              "name": "scratch",
              "kind": "tmp",
              "mountPath": "/tmp/scratch",
              "sizeLimit": "8Gi"
            }
          ],
          "secrets": [
            {
              "name": "linear-api-key",
              "secretProviderClass": "linear-spc",
              "mountPath": "/home/criteria/linear-secrets",
              "env": {
                "LINEAR_API_KEY": "linear_api_key"
              }
            },
            {
              "name": "github-tokens",
              "secretProviderClass": "copilot-spc",
              "mountPath": "/home/criteria/secrets",
              "env": {
                "WORKFLOW_GITHUB_TOKEN": "workflow_github_token",
                "REVIEWER_GITHUB_TOKEN": "reviewer_github_token"
              }
            }
          ]
        },
        "linear-triage-url": {
          "type": "url",
          "url": "git::https://github.com/brokenbots/workflow-example.git//linear_triage_v1?ref=91153bc90b8fb48cec215c2a4cbc425dd11b7821",
          "ref": "91153bc90b8fb48cec215c2a4cbc425dd11b7821",
          "class": "triage",
          "namespace": "criteria-jobs",
          "volumes": [
            {
              "name": "data",
              "kind": "pvc",
              "claim": "criteria-data",
              "mountPath": "/data",
              "env": {
                "CRITERIA_RUN_DIR_ROOT": "/data/.criteria/runs",
                "CRITERIA_SHELL_ALLOWED_PATHS": "/data"
              }
            },
            {
              "name": "repo",
              "kind": "pvc",
              "claim": "criteria-repo",
              "mountPath": "/repo"
            },
            {
              "name": "scratch",
              "kind": "tmp",
              "mountPath": "/tmp/scratch",
              "sizeLimit": "8Gi"
            }
          ],
          "secrets": [
            {
              "name": "linear-api-key",
              "secretProviderClass": "linear-spc",
              "mountPath": "/home/criteria/linear-secrets",
              "env": {
                "LINEAR_API_KEY": "linear_api_key"
              }
            },
            {
              "name": "github-tokens",
              "secretProviderClass": "copilot-spc",
              "mountPath": "/home/criteria/secrets",
              "env": {
                "WORKFLOW_GITHUB_TOKEN": "workflow_github_token",
                "REVIEWER_GITHUB_TOKEN": "reviewer_github_token"
              }
            }
          ]
        },
        "ticket-cleanup-url": {
          "type": "url",
          "url": "git::https://github.com/brokenbots/workflow-example.git//ticket_cleanup_v1?ref=0b8136650d5622381bf18a597ed93b8c272ba19b",
          "ref": "0b8136650d5622381bf18a597ed93b8c272ba19b",
          "class": "triage",
          "namespace": "criteria-jobs",
          "volumes": [
            {
              "name": "data",
              "kind": "pvc",
              "claim": "criteria-data",
              "mountPath": "/data",
              "env": {
                "CRITERIA_RUN_DIR_ROOT": "/data/.criteria/runs",
                "CRITERIA_SHELL_ALLOWED_PATHS": "/data"
              }
            },
            {
              "name": "repo",
              "kind": "pvc",
              "claim": "criteria-repo",
              "mountPath": "/repo"
            },
            {
              "name": "scratch",
              "kind": "tmp",
              "mountPath": "/tmp/scratch",
              "sizeLimit": "8Gi"
            }
          ],
          "secrets": [
            {
              "name": "linear-api-key",
              "secretProviderClass": "linear-spc",
              "mountPath": "/home/criteria/linear-secrets",
              "env": {
                "LINEAR_API_KEY": "linear_api_key"
              }
            },
            {
              "name": "github-tokens",
              "secretProviderClass": "copilot-spc",
              "mountPath": "/home/criteria/secrets",
              "env": {
                "WORKFLOW_GITHUB_TOKEN": "workflow_github_token",
                "REVIEWER_GITHUB_TOKEN": "reviewer_github_token"
              }
            }
          ]
        },
        "kanboard-triage-url": {
          "type": "url",
          "url": "git::https://github.com/brokenbots/workflow-example.git//kanboard_triage_v1?ref=cf54d6fc0291464fc966bef6c6aff0c382a38701",
          "ref": "cf54d6fc0291464fc966bef6c6aff0c382a38701",
          "class": "triage",
          "namespace": "criteria-jobs",
          "volumes": [
            {
              "name": "data",
              "kind": "pvc",
              "claim": "criteria-data",
              "mountPath": "/data",
              "env": {
                "CRITERIA_RUN_DIR_ROOT": "/data/.criteria/runs",
                "CRITERIA_SHELL_ALLOWED_PATHS": "/data"
              }
            },
            {
              "name": "repo",
              "kind": "pvc",
              "claim": "criteria-repo",
              "mountPath": "/repo"
            },
            {
              "name": "scratch",
              "kind": "tmp",
              "mountPath": "/tmp/scratch",
              "sizeLimit": "8Gi"
            }
          ],
          "secrets": [
            {
              "name": "github-tokens",
              "secretProviderClass": "copilot-spc",
              "mountPath": "/home/criteria/secrets",
              "env": {
                "WORKFLOW_GITHUB_TOKEN": "workflow_github_token",
                "REVIEWER_GITHUB_TOKEN": "reviewer_github_token"
              }
            },
            {
              "name": "kanboard-secrets",
              "secretProviderClass": "kanboard-spc",
              "mountPath": "/home/criteria/kanboard-secrets",
              "env": {
                "KANBOARD_APP_TOKEN": "kanboard_app_token",
                "KANBOARD_URL": "kanboard_url"
              }
            }
          ]
        },
        "kanboard-develop-url": {
          "type": "url",
          "url": "git::https://github.com/brokenbots/workflow-example.git//kanboard_develop_v1?ref=cf54d6fc0291464fc966bef6c6aff0c382a38701",
          "ref": "cf54d6fc0291464fc966bef6c6aff0c382a38701",
          "namespace": "criteria-jobs",
          "volumes": [
            {
              "name": "data",
              "kind": "pvc",
              "claim": "criteria-data",
              "mountPath": "/data",
              "env": {
                "CRITERIA_RUN_DIR_ROOT": "/data/.criteria/runs",
                "CRITERIA_SHELL_ALLOWED_PATHS": "/data"
              }
            },
            {
              "name": "repo",
              "kind": "pvc",
              "claim": "criteria-repo",
              "mountPath": "/repo"
            },
            {
              "name": "scratch",
              "kind": "tmp",
              "mountPath": "/tmp/scratch",
              "sizeLimit": "8Gi"
            }
          ],
          "secrets": [
            {
              "name": "github-tokens",
              "secretProviderClass": "copilot-spc",
              "mountPath": "/home/criteria/secrets",
              "env": {
                "WORKFLOW_GITHUB_TOKEN": "workflow_github_token",
                "REVIEWER_GITHUB_TOKEN": "reviewer_github_token"
              }
            },
            {
              "name": "kanboard-secrets",
              "secretProviderClass": "kanboard-spc",
              "mountPath": "/home/criteria/kanboard-secrets",
              "env": {
                "KANBOARD_APP_TOKEN": "kanboard_app_token",
                "KANBOARD_URL": "kanboard_url"
              }
            }
          ]
        }
      },
      "routes": [
        {
          "name": "criteria-triage",
          "workflow": "linear-triage-url",
          "project": "Criteria K8s Workflow Runner",
          "states": [
            "Triage"
          ]
        },
        {
          "name": "criteria-develop",
          "workflow": "linear-develop-url",
          "project": "Criteria K8s Workflow Runner",
          "states": [
            "Ready for Development"
          ]
        },
        {
          "name": "criteria-cleanup",
          "workflow": "ticket-cleanup-url",
          "project": "Criteria K8s Workflow Runner",
          "tags": [
            "criteria-dirty"
          ],
          "states": [
            "Ready for Development"
          ]
        },
        {
          "name": "kanboard-triage",
          "workflow": "kanboard-triage-url",
          "project": "Kanboard Tickets",
          "states": [
            "Backlog"
          ]
        },
        {
          "name": "kanboard-develop",
          "workflow": "kanboard-develop-url",
          "project": "Kanboard Tickets",
          "states": [
            "Ready"
          ]
        }
      ]
    }
